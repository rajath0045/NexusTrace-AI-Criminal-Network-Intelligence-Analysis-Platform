import { EmptyState } from "@/components/ui/empty-state";

export function ProfileRelationships() {
  return (
    <EmptyState
      title="No relationship view in this phase"
      description="Verified relationship exploration will appear here after the evidence-backed graph workflow is available."
    />
  );
}
